import { readFileSync } from "node:fs";

export function solve(input: string): string {
  // Leia os intervalos e retorne "total maior" após considerar sua união.
  void input;
  return "0 0";
}

console.log(solve(readFileSync(0, "utf8")));
