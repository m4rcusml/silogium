import { readFileSync } from "node:fs";

export function solve(input: string): string {
  // Leia a rede e retorne "distancia quantidade", conforme o enunciado.
  void input;
  return "-1 0";
}

console.log(solve(readFileSync(0, "utf8")));
