import { readFileSync } from "node:fs";

export function solve(input: string): string {
  // Leia n, o alvo e os volumes. Retorne os índices escolhidos ou "-1".
  // Respeite a ordem original e os dois critérios de desempate do enunciado.
  void input;
  return "-1";
}

console.log(solve(readFileSync(0, "utf8")));
